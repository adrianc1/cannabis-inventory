<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link href="/tailwind.css" rel="stylesheet" />
		<title>Sign Up Form</title>
	</head>
	<body>
		<%- include("../partials/header.ejs") %>
		<h1 class="text-3xl font-bold underline mx-auto my-8 text-center">
			Sign Up For Greenventory
		</h1>
		<%- include("../partials/errors.ejs") %>
		<form
			action="/signup/create-account"
			method="POST"
			class="max-w-2xl mx-auto p-8 bg-white shadow-lg rounded-xl border border-gray-200"
		>
			<h2 class="text-2xl font-bold mb-6 text-gray-800">Create Account</h2>

			<div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
				<div class="flex flex-col gap-2">
					<label for="firstName" class="text-sm font-semibold text-gray-600"
						>First Name</label
					>
					<input
						type="text"
						id="firstName"
						name="firstName"
						value="<%= locals.firstName %>"
						class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
					/>
				</div>

				<div class="flex flex-col gap-2">
					<label for="lastName" class="text-sm font-semibold text-gray-600"
						>Last Name</label
					>
					<input
						type="text"
						id="lastName"
						name="lastName"
						value="<%= locals.lastName %>"
						class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
					/>
				</div>

				<div class="flex flex-col gap-2 md:col-span-2">
					<label for="email" class="text-sm font-semibold text-gray-600"
						>Email Address</label
					>
					<input
						type="email"
						id="email"
						name="email"
						value="<%= locals.email %>"
						class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
					/>
				</div>
				<div class="flex flex-col gap-2">
					<label for="password" class="text-sm font-semibold text-gray-600"
						>Password</label
					>
					<input
						type="password"
						id="password"
						name="password"
						class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
					/>
				</div>

				<div class="flex flex-col gap-2">
					<label
						for="confirmPassword"
						class="text-sm font-semibold text-gray-600"
						>Confirm Password</label
					>
					<input
						type="password"
						id="confirmPassword"
						name="confirmPassword"
						class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
					/>
				</div>
				<div class="md:col-span-2 border-t pt-4 mt-2">
					<h3 class="text-sm font-bold text-gray-400 uppercase tracking-wider">
						Business Information
					</h3>
				</div>

				<div class="flex flex-col gap-2">
					<label for="company" class="text-sm font-semibold text-gray-600"
						>Company</label
					>
					<input
						type="text"
						id="company"
						name="company"
						class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
					/>
				</div>
				<div class="flex flex-col gap-2">
					<label for="license" class="text-sm font-semibold text-gray-600"
						>License Number</label
					>
					<input
						type="text"
						id="license"
						name="license"
						class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
					/>
				</div>
			</div>

			<div class="flex justify-center mt-6">
				<button
					type="submit"
					class="w-full md:w-64 bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-3 rounded-lg shadow-md hover:shadow-lg transition-all cursor-pointer"
				>
					Create Account
				</button>
			</div>
		</form>
	</body>
</html>
