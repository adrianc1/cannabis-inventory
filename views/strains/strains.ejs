<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link href="/tailwind.css" rel="stylesheet" />
		<title>Strains Page</title>
	</head>
	<body>
		<%- include ('../partials/header.ejs') %>

		<div class="mb-8 mt-4 relative">
			<h1
				class="absolute left-1/2 -translate-x-1/2 top-1/2 -translate-y-1/2 text-3xl font-bold text-gray-800"
			>
				<%= message %>
			</h1>
		</div>

		<div class="main-buttons">
			<button class="btn-primary">
				<a href="/strains/create-strain">Create New Strain</a>
			</button>

			<div class="view-buttons">
				<button class="btn-primary"><a href="/strains/">See Strains</a></button>
				<button class="btn-primary">
					<a href="/products/">See Products</a>
				</button>
			</div>
		</div>

		<table class="products-table">
			<tr class="products-table-headers">
				<th class="table-header">Name</th>
				<th class="table-header">Description</th>
				<th class="table-header">Type</th>
			</tr>
			<% strains.map((strain) => { %>
			<tr class="product-row">
				<td><a href="/strains/<%=strain.id%>"><%=strain.name %></a></td>
				<td><%= strain.description %></td>
				<td><%=strain.type%></td>
			</tr>
			<% }) %>
		</table>
	</body>
</html>
